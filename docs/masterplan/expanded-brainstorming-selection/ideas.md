# Superseded Brainstorm Notes

The earlier idea funnel explored page-based idea selection, candidate IDs, and larger brainstorming batches. Transcript review showed that direction solves the wrong problem first.

Primary fix now: anchor Step B1 before brainstorming so audit/review prompts, deferred tasks, and cross-repo ownership boundaries cannot drift into broad feature planning.

Keep these notes only as historical context:

- More ideas per cycle may still be useful when `brainstorm_anchor.mode == feature-ideas`.
- Selection UI must degrade to structured gates in Codex; do not depend on native multi-select or arbitrary ID-entry UI.
- The first shipped fix is the intent anchor, not the idea-selection funnel.
